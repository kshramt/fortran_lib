language: Fortran
rvm:
  - 1.9.3
before_install:
  - sudo apt-get -qq install ${MY_GFORTRAN}
  - gem install fort
  - ruby -e 'require "pp"; pp $LOAD_PATH'
  - ruby -e "p (require 'fort')"
script:
  - cd src
  - rake test --trace
env:
  - MY_ERB="erb -T - -P" MY_GFORTRAN=gfortran-4.6
